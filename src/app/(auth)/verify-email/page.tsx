import { EmailVerifyContent } from './EmailVerifyContent';

export const metadata = {
  title: 'Verify Email - OMRS',
};

export default function VerifyEmailPage() {
  return <EmailVerifyContent />;
}
